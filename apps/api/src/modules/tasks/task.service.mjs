import { publish } from "../../infrastructure/events/event-bus.mjs";
import { taskRepository } from "./task.repository.mjs";
export async function runTask(taskId) { const task = await taskRepository.findById(taskId); if (!task) throw Object.assign(new Error("Task not found"), { statusCode: 404 }); task.status = "IN_PROGRESS"; const saved = await taskRepository.save(task); publish("AGENT_RUN_STARTED", task.project_id, { taskId, agentId: task.assignee_agent_id }); setTimeout(async () => { task.status = "IN_REVIEW"; await taskRepository.save(task); publish("AGENT_RUN_COMPLETED", task.project_id, { taskId, agentId: task.assignee_agent_id }); }, 800); return saved; }
