import type { Message } from "../../types/office";
export function ConversationPanel({ messages, draft, onDraftChange, onSend }: { messages: Message[]; draft: string; onDraftChange: (value: string) => void; onSend: () => void }) { return <section className="panel conversation"><div className="panel-title"><h2>CEO Conversation</h2><span className="muted">{messages.length} messages</span></div><div className="messages">{messages.slice(-5).map((message, index) => <div className={`message ${message.role}`} key={`${message.createdAt}-${index}`}><b>{message.role}</b><p>{message.content}</p></div>)}</div><form onSubmit={(event) => { event.preventDefault(); onSend(); }}><input value={draft} onChange={(event) => onDraftChange(event.target.value)} placeholder="업무를 지시하세요. 예: 로그인 화면을 구현해줘" /><button type="submit">전송</button></form></section>; }

