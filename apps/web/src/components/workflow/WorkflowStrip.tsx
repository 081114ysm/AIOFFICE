import type { WorkflowStage } from "../../types/office";
const stages: { id: WorkflowStage; label: string }[] = [{ id: "RESEARCH", label: "시장조사" }, { id: "ANALYSIS", label: "브랜드 분석" }, { id: "IDEATION", label: "TOP 3 기획" }, { id: "QA", label: "브랜드 QA" }, { id: "APPROVAL", label: "대표 승인" }, { id: "SCRIPT", label: "대본" }, { id: "PRODUCTION", label: "제작" }, { id: "REVIEW", label: "성과 리뷰" }];
export function WorkflowStrip({ stage = "RESEARCH" }: { stage?: WorkflowStage }) { const activeIndex = stages.findIndex((item) => item.id === stage); return <section className="workflow-strip"><div className="workflow-heading"><span className="eyebrow">TODAY'S WORKFLOW</span><span className="muted">대표 승인 1회 · 현재: {stages[activeIndex]?.label ?? "시장조사"}</span></div><div className="workflow-steps">{stages.map((item, index) => <div className={`workflow-step ${index === activeIndex ? "active" : ""} ${index < activeIndex ? "complete" : ""}`} key={item.id}><span className="workflow-index">{String(index + 1).padStart(2, "0")}</span><strong>{item.label}</strong>{index < stages.length - 1 && <span className="workflow-arrow">→</span>}</div>)}</div></section>; }
